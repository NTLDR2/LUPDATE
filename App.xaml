<Application x:Class="LUpdate.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Office 2000 Colors -->
        <Color x:Key="ClassicGray">#D4D0C8</Color>
        <Color x:Key="ClassicDark">#808080</Color>
        <Color x:Key="ClassicLight">#FFFFFF</Color>
        <Color x:Key="ClassicBlue">#0A246A</Color>

        <Style TargetType="Menu">
            <Setter Property="Background" Value="#D4D0C8"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#808080"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="4,2,4,2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Border"
                                Background="Transparent"
                                BorderThickness="1"
                                BorderBrush="Transparent"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="22" SharedSizeGroup="IconCol"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ArrowCol"/>
                                </Grid.ColumnDefinitions>

                                <!-- Icon Area -->
                                <ContentPresenter Grid.Column="0" 
                                                  x:Name="Icon" 
                                                  ContentSource="Icon" 
                                                  Width="16" Height="16" 
                                                  Margin="3,0,3,0"
                                                  VerticalAlignment="Center"/>

                                <!-- Header Area -->
                                <ContentPresenter Grid.Column="1" 
                                                  ContentSource="Header"
                                                  Margin="5,2,10,2"
                                                  RecognizesAccessKey="True"
                                                  VerticalAlignment="Center"/>

                                <!-- Arrow for submenus -->
                                <Path Grid.Column="2" 
                                      x:Name="Arrow"
                                      Data="M 0,0 L 4,4 L 0,8 Z"
                                      Fill="#333"
                                      Margin="5,0,5,0"
                                      VerticalAlignment="Center"
                                      Visibility="Collapsed"/>

                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsSubmenuOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="None">
                                    <Border Background="#D4D0C8"
                                            BorderThickness="1"
                                            BorderBrush="#808080">
                                        <Grid Margin="2">
                                            <StackPanel IsItemsHost="True"
                                                        KeyboardNavigation.DirectionalNavigation="Cycle" />
                                        </Grid>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#0A246A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="Arrow" Property="Fill" Value="White"/>
                                <!-- 3D Raised Effect -->
                                <Setter TargetName="Border" Property="BorderThickness" Value="1"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#808080"/>
                            </Trigger>
                            <Trigger Property="Role" Value="TopLevelHeader">
                                <Trigger.EnterActions>
                                </Trigger.EnterActions>
                            </Trigger>
                            <Trigger Property="IsSubmenuOpen" Value="true">
                                <!-- 3D Sunken Effect for top-level headers when open -->
                                <Setter TargetName="Border" Property="BorderBrush" Value="#808080"/>
                                <Setter TargetName="Border" Property="Background" Value="#D4D0C8"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="true">
                                <Setter TargetName="Arrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#808080"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
