<Window x:Class="LUpdate.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="About LiveUpdate" Height="400" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#D4D0C8">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Image Source="pack://application:,,,/LUPDATE.ico" Height="48" Width="48" Margin="0,0,15,0"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="LiveUpdate Utility" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="Version 0.1.0"/>
            </StackPanel>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="This program is free software: you can redistribute it and/or modify it under the terms of the GNU General Public License as published by the Free Software Foundation, either version 3 of the License, or (at your option) any later version." TextWrapping="Wrap" Margin="0,0,0,10" FontSize="11"/>

        <TextBox Grid.Row="2" x:Name="txtLicense" IsReadOnly="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" FontSize="10" Background="White" BorderBrush="#808080"/>

        <Button Grid.Row="3" Content="OK" Width="75" Height="23" HorizontalAlignment="Right" Margin="0,15,0,0" Click="btnOk_Click" IsDefault="True"/>
    </Grid>
</Window>
